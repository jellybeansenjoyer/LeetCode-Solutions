#include<bits/stc++.h>
using namespace std;
vector<int> numberOfLines(vector<int>& widths, string s) {
    int arr[2];
    int lines=1;
    int width_last_line=0;
    int sum = 0;
    for(auto i =0;i<widths.size();i++){
        if(sum<100){
            sum+=width[i]
        }
    }        
}